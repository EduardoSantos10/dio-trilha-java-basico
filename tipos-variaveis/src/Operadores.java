public class Operadores {
    public static void main(String[] args) {
    // * Atribuição -> (=)

      
        double soma = 10.5 + 15.7;
        int subtracao = 113 - 25;
        int multiplicacao = 20 * 7;
        int divisao = 15 / 3;
        int modulo = 18 % 3; //-> a div dessa expressão resultara em numeros fracionários (restos de div)
        double resultado = (10 * 7) + (20 / 4);
    

        //String nomeCompleto = "Eduardo" + "Santos"; // simbolo de adição (+), concatena as palavras que foram declaradas em strings.
        //System.out.println(nomeCompleto);

       /* String concatenacao = "?";

        concatenacao = 5+5+5+"5"; // Primeiro ele realiza a soma dos três valores e depois concatena a string
        System.out.println(concatenacao);
        
        concatenacao = "5"+(5+5+5); // Ele adiciona a concatenação e após isso, ele soma seus valores
        System.out.println(concatenacao);
        
        concatenacao = 5+5+"5"+5; // Ele realiza primeira a soma que resulta em 10, após isso, concatena com 5, e o último valor vira adição de 5. 
        System.out.println(concatenacao);
        
        concatenacao = "5"+5+"5"+5; // Por ter uma string no começo da soma, ele apenas vai concatenar todos dos valores, sendo assim, não sendo realizada soma.
        System.out.println(concatenacao);
        
        concatenacao = 5+"5"+5+5; // A partir do momento que ele detecta um caractére, ele não realiza mais uma soma, apenas concatenação.
        System.out.println(concatenacao);*/

        /* *int numero = 10;
        System.out.println(-numero); // tenho um numero positivo atribuido, porém quando vou chamar a Var, eu coloco sinal de negativo e o sistema imprimi o número negativo.

        int number = -20;
        System.out.println(-number); // após ter atribuido sinal de negativo no valor da minha Var, eu chamo a Var com sinal negativo e automaticamente torna ela positiva(- com - = +)
    
        numero = -numero; // através dessa atribuição, eu torno meu número de positivo a negativo até o final da conta.
        numero = numero * -1; // através dessa atribuição, eu torno meu número negativo em positivo.*/
    

        // Incrementação
        //repetição(x)
        //int numero = 10; // acrescentar um valor (soma), na minha variavél.
        //numero = numero + 10;

        //numero++;

        //System.out.println( ++numero); // nesse caso ela está antes da execução do prog. por isso e adicionado a soma. Com valores em 1

        //decrementação

        //System.out.println(numero-- );
        //System.out.println(numero); // ele está realizando o decremento antes de terminar o prog. e ele vai imprimir novamente, já com o valor subtraido. Com valores em 1
    


        // Maniapular expressões booleanas
        boolean status = true;

        status = !status; // com essa atribuição eu torno ela totalmente falsa, até o término do meu programa.
        // System.out.println(!status); // através do ponto de exclamação, eu torno a minha variavél que já é 'true', em 'false'.
        System.out.println(status); // aqui ela está verdadeira.




    }
    
}
